# Core dependencies
openai>=1.0.0
python-dotenv>=1.0.0
pandas>=2.0.0
numpy>=1.24.0

# Vector search (optional but recommended for scalability)
faiss-cpu>=1.7.0

# Fuzzy string matching (for sparse team name matching)
rapidfuzz>=3.0.0

# Database (sqlite3 is built-in)

# CLI
click>=8.0.0
rich>=13.0.0

# Testing
pytest>=7.0.0
